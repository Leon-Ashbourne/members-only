# Members only
A simple members-only project. Only the privilaged can move freely. 

## Prerequisites
The following instructions assume that the user has already installed **Node.js (v22 or higher)** and **npm (v11 or higher)**. For the database, we’re using **PostgreSQL (v14 or higher)**. 
You are advised to include your own **.env** file configured appropriately for your local environment

## How to use
1. Install the dependencies by running `npm install`.
2. Then, run `node app.js` to build the application.

Congrats, you are all set to run the application. 

## conclusion
This is an assignment done following the instructions from [The odin Project](https://www.theodinproject.com/lessons/node-path-nodejs-members-only)
